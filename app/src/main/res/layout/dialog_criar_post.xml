<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="20dp">

    <EditText
        android:id="@+id/popup_edt_nome"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:background="@android:drawable/editbox_background"
        android:hint="Seu nome (Opcional)"
        android:inputType="textPersonName"
        android:paddingHorizontal="12dp" />

    <CheckBox
        android:id="@+id/popup_cb_anonimo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="Postar como Anônimo"
        android:textColor="#3C3C6C"
        android:textSize="14sp" />

    <EditText
        android:id="@+id/popup_edt_pergunta"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:layout_marginTop="16dp"
        android:background="@android:drawable/editbox_background"
        android:gravity="top|start"
        android:hint="Qual a sua dúvida ou desabafo hoje?"
        android:inputType="textMultiLine"
        android:padding="12dp" />

</LinearLayout>